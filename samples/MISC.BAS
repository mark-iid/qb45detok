' Numeric and misc functions the corpus never reaches
X! = 2.5
PRINT SGN(X!); SQR(X!); EXP(X!); LOG(X!)
PRINT ATN(X!); TAN(X!); FIX(X!); CINT(X!)
PRINT CLNG(X!); CSNG(X!); CDBL(X!)
PRINT DATE$; TIME$
PRINT UCASE$("abc"); LCASE$("ABC")
PRINT OCT$(64); HEX$(255)
BEEP
FOR I% = 1 TO 5
        IF I% = 3 THEN EXIT FOR
NEXT I%
DO
        EXIT DO
LOOP
WHILE 0
WEND
END
