DEFINT A-Z
DIM Arr(100)
SCREEN 1
OPEN "T.DAT" FOR OUTPUT AS 1
PRINT #1,
PRINT #1, "a";
PRINT #1, "a",
WRITE #1
PRINT TAB(5), "x"
PRINT USING "##"; 1,
LPRINT USING "##"; 1;
PRINT ,
PRINT ;
CLOSE
PSET STEP(1, 1)
PRESET STEP(1, 1), 2
PAINT STEP(1, 1), 1
LINE STEP(0, 0)-STEP(5, 5)
GET (0, 0)-(4, 4), Arr
GET STEP(0, 0)-STEP(4, 4), Arr
PUT STEP(0, 0), Arr
PUT STEP(0, 0), Arr, XOR
CIRCLE STEP(5, 5), 3
WHILE X < 2
  X = X + 1
WEND
END
